package com.noxx.calculator;

import android.widget.TextView;

/**
 * Created by HB on 17/11/2016.
 */

/*public class CalculatorManager {


    private double value1;
    private double result;
    private double save;
    private TextView screen;
    private String existantOperator="";



    public String calcul(String operator, String display){
        String returnDisplay;

        if (existantOperator.equals("")){
            existantOperator=operator;
            returnDisplay=display;
        } else {

            if (operator.equals("+")) {

              result=save+Double.valueOf(display);
            }

            if (operator.equals("-")) {

                result=save-Double.valueOf(display);
            }

            if (operator.equals("*")) {

                result=save*Double.valueOf(display);
            }

            if (operator.equals("/")) {

                result=save/Double.valueOf(display);

            }
            returnDisplay=String.valueOf(result);
        }
        save=Double.valueOf(display);
        return returnDisplay;

    }

}*/
